# Changesets

Add a changeset when modifying either public design-system package:

```bash
pnpm changeset
```

`@assalabs/design-system-tools` and `@assalabs/create-design-system` are
versioned and released together. Generated themes, UI packages, applications,
and examples remain private consumer source.
