# {{DESIGN_SYSTEM_NAME}} web UI

Base UI components styled with CSS Modules and the semantic tokens from
`{{PACKAGE_SCOPE}}/theme`.

Import `{{PACKAGE_SCOPE}}/theme/css` once in the web application, then import
components from this package. The component source remains inside the workspace
so product teams can change its API and visual treatment directly.
